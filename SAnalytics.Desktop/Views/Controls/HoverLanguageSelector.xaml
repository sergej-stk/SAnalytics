<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="SAnalytics.Desktop.Views.Controls.HoverLanguageSelector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Button
        x:Name="LanguageButton"
        Width="40"
        Height="40"
        Background="Transparent"
        BorderBrush="Transparent"
        CornerRadius="20">

        <Button.Content>
            <FontIcon
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="18"
                Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                Glyph="&#xF2B7;" />
        </Button.Content>

        <Button.Flyout>
            <Flyout x:Name="LanguageFlyout" Placement="Bottom">
                <StackPanel Spacing="8">
                    <TextBlock
                        HorizontalAlignment="Center"
                        Style="{ThemeResource BodyStrongTextBlockStyle}"
                        Text="{x:Bind ViewModel.LanguageLabel, Mode=OneWay}" />

                    <ComboBox
                        x:Name="LanguageComboBox"
                        MinWidth="150"
                        HorizontalAlignment="Stretch"
                        DisplayMemberPath="NativeName"
                        ItemsSource="{x:Bind ViewModel.AvailableLanguages}"
                        SelectedItem="{x:Bind ViewModel.SelectedLanguage, Mode=TwoWay}" />
                </StackPanel>
            </Flyout>
        </Button.Flyout>
    </Button>
</UserControl>